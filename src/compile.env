if [ -z "${BOSH_PACKAGES_DIR:-}" ]; then
  export RUST_HOME=$(readlink -nf /var/vcap/packages/${PACKAGE_NAME})
else
  export RUST_HOME=$BOSH_PACKAGES_DIR/${PACKAGE_NAME}
fi

export CARGO_HOME="${RUST_HOME}/.cargo"
export PATH="${RUST_HOME}/bin:${CARGO_HOME}/bin:${PATH}"
